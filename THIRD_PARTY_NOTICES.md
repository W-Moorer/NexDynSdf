# Third-party notices

The SdfLib Gear model under `models/sdflib/` is a third-party test and benchmark
asset distributed under the SdfLib MIT license. Its license text is preserved
at `models/licenses/SdfLib-LICENSE.txt`.

The models under `models/pycoco/` and `models/nagata/` are prior work authored
by the NexDynSdf repository owner. They are first-party assets, so no separate
third-party license files are included for them. Exact source revisions, paths,
and SHA-256 hashes are recorded in `models/MANIFEST.md`.

The implementation is informed by the two Pujol and Chica papers cited in
`docs/reference-provenance.md`. No third-party runtime library is linked.
